kind = "web"
previewPath = "/"
title = "Exchange Admin"
version = "1.0.0"
id = "artifacts/exchange-admin"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25489

[services.development]
run = "pnpm --filter @workspace/exchange-admin run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/exchange-admin", "run", "build" ]
publicDir = "artifacts/exchange-admin/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25489"
BASE_PATH = "/"
